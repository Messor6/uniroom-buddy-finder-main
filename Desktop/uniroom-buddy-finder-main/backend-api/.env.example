# Configuração do Servidor
PORT=3001
NODE_ENV=development

# Configuração do Banco de Dados
# Para MongoDB local:
MONGODB_URI=mongodb://localhost:27017/uniroom-buddy-finder

# Para MongoDB Atlas (substitua com suas credenciais):
# MONGODB_URI=mongodb+srv://<SEU_USUARIO>:<SUA_SENHA>@<SEU_CLUSTER>.mongodb.net/uniroom-buddy-finder

# Configuração JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Configuração CORS
FRONTEND_URL=http://localhost:3000

# ⚠️ IMPORTANTE:
# 1. Copie este arquivo para .env
# 2. Substitua os valores pelos seus dados reais
# 3. NUNCA commite o arquivo .env com credenciais reais
